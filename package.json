{
  "name": "react-copyx-monorepo",
  "private": true,
  "version": "0.0.2",
  "scripts": {
    "release": "pnpm --filter react-copyx version patch && pnpm --filter react-copyx build && npm publish ./react-copyx --access public && git add react-copyx/package.json && git commit -m 'chore(release): release new version' && git tag v$(node -p \"require('./react-copyx/package.json').version\") && git push && git push --tags"
  },
  "workspaces": [
    "demo-project",
    "react-copyx"
  ],
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.13",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  }
}
